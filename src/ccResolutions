#& -l --compiler g++-4.6 --args "-Wall -g -Werror -Wstrict-null-sentinel -Wformat-security -W -Wwrite-strings -Wshadow -ansi -DVERSION=\\\"2.0.3\\\" -std=c++0x"
#Left out -Wconversion and -pedantic because of the ugly yylex.cc
# In the future, -std=c++0x is going to be default.
# This file now requires ccbuild > 1.5.7
#	Some options have no resolution, they should
#	be installed on any POSIX Linux system and don't
#  require extra options, for example: Flex, GNU GetOpt
FlexLexer.h	
getopt.h	
gcrypt.h	`libgcrypt-config --cflags --libs`
errno.h	
glob.h	
inttypes.h	
sys/stat.h	
sys/time.h	
sys/types.h	
unistd.h	
omp.h	-fopenmp -lgomp
bobcat/process	-lbobcat
bobcat/user	-lbobcat
sys/utsname.h	
features.h	
stddef.h	
boost/circular_buffer.hpp	
boost/lexical_cast.hpp	
openssl/evp.h	`pkg-config --libs --cflags openssl`
gnutls/openssl.h	-lgnutls-openssl
