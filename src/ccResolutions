#& -l -p --args "-Wall -g -Werror --ansi" --md5
#	Some options have no resolution, they should
#	be installed on any POSIX Linux system.
# Dependency !Flex
#	The quotes are only there to show the tab seperation.
FlexLexer.h	""
getopt.h	""

